{% extends 'lwLayout.html.twig' %}

{% block body %}

    <section class="title_page">

        <div class="section_content">

            <h1>Valider votre inscrption!</h1>

    <section/>
    <div class="body_content connexion">
        <div id="connexion_form">
            <div class="form_obs">
            {{ form_start(form, {'attr': {'class': 'col-sm-6 col-sm-offset-6'}}) }}
            {{ form_errors(form) }}
                <div class="form-group">
                    {{ form_label(form.name, 'Votre Nom') }}
                    {{ form_errors(form.name) }}
                    {{ form_widget(form.name, {'attr': {'class': 'form-control', 'id': 'nom', 'required': 'required', 'placeholder': 'Nom'}}) }}
                    <span id="nomVide"></span>
                </div>
                <div class="form-group">
                    {{ form_label(form.firstname, 'Votre Prénom') }}
                    {{ form_errors(form.firstname) }}
                    {{ form_widget(form.firstname, {'attr': {'class': 'form-control', 'id': 'prenom', 'placeholder': 'Prénom'}}) }}
                </div>
                <div>
                    <button type="submit" class="btn btn-success submit">
                        Enregistrer
                    </button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}